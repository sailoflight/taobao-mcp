<!-- agent-project-guides:adapter-trigger:start -->
## Required package adaptation

Overrides ordinary routing for Development adaptation. Trigger revision: {{PACKAGE_REVISION}}. Source: {{PACKAGE_REPOSITORY}}.

0. Do not re-read the injected root unless this block changes. Before package reads, verify `{{GUIDES_PATH}}/{PACKAGE_VERSION,PACKAGE_REMOTE.json,scripts/install.sh,routing/development.roles.jsonl,routing/project-types.jsonl}`. If missing, ask structured `package_missing`: restore vendored Source, remove managed blocks, or stop; give impacts and wait. Absence is never current.
1. If trigger/local revisions differ, run `{{GUIDES_PATH}}/scripts/install.sh trigger --target <project-root>` and re-read root. Run read-only `check-update` automatically. Continue only for `current`; for `remote_differs/unavailable`, ask `package_freshness`: sync/retry, explicitly use the stated local revision, or stop. Then honor state: `adapted` -> remove-trigger and original task; `partial` -> resume only recorded scope/reason; `blocked` -> ask retry/narrow/skip/remove.
2. Before repository discovery, explicit compatible role/mode wins: Maintainer -> `readapt`, Developer -> `initialize`; do not classify it. Only if unassigned, use one bounded root inventory: new/empty -> Developer, existing -> Maintainer, ambiguity uses the root question protocol. Reuse this inventory for type evidence; then read only targeted build/entry/test/CI/doc files, never a whole-tree scan.
3. Never Read/cat a registry. Exact-grep one quoted `id` or assigned literal alias in `{{GUIDES_PATH}}/routing/development.roles.jsonl`; do not read `planes.jsonl`. Read only its guide and adaptation procedure, resolving both under `{{GUIDES_PATH}}/`, never relative to the registry or cwd; failure is package integrity, not permission to glob.
4. Select one scoped primary type: `mcp`, `library`, `cli`, `service`, `application-ui`, `data-automation`, or `monorepo`. Exact-grep its quoted `id` in `{{GUIDES_PATH}}/routing/project-types.jsonl`, then read only its profile. Monorepo applies only to root composition; classify later package scopes separately. If no type is exact or one indivisible scope materially matches several, ask `project_type`: confirm closest, split scope, update the package, or declare it inapplicable; wait. Never compare by reading multiple profiles.
5. Follow the procedure's artifact decisions. Read one exact template immediately before merging/creating one artifact, finish and verify it, then consider another; never preload templates or create empty documents. Preserve original root content and parallel changes.
6. After verification set `adapted` with UTC/scope/reason, then `remove-trigger`; re-read `{{ROOT_INSTRUCTIONS}}` and route the original task. On unsafe completion set `blocked` and retain trigger.
<!-- agent-project-guides:adapter-trigger:end -->
